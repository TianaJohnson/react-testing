import React from 'react';
import './Header.scss';
import Logo from ''

const Header = (props) => {
    return (
        <header>
            <div className="wrapper">
                <div className="logo">

                </div>
            </div>
        </header>
    )
};

export default Header;